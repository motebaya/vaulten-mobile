<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for splash/system UI. Compose handles the actual app theming. -->
    <style name="Theme.Vaulten" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Transparent system bars for edge-to-edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Note: FLAG_SECURE is set programmatically in MainActivity.kt -->
        <!-- android:windowSecure is NOT a valid theme attribute -->
    </style>
</resources>
